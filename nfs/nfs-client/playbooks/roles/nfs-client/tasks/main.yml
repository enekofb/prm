- name: Ensure NFS common is installed.
  apt: name=nfs-common state=installed update_cache=yes

- name: Create mountable dir
  file: path=/nfs state=directory mode=777 owner=root group=root

- name: set mountpoints
  mount: name=/home/samba/shares/owncloud src="192.168.1.70:/var/owncloud/data" fstype=nfs opts=defaults dump=0 passno=2 state=mounted